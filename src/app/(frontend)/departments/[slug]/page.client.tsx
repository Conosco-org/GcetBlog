'use client'

import { useEffect } from 'react'

export default function DepartmentPageClient() {
  useEffect(() => {
    // Inject department theme CSS vars if needed (future enhancement)
  }, [])
  return null
}
